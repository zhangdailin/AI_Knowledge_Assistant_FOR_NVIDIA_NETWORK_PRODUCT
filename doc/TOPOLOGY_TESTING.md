# 拓扑还原功能完整测试指南

## 概述

本文档提供拓扑还原功能的完整测试流程，包括：
- 基本功能测试
- IB 网络拓扑测试
- RoCE 网络拓扑测试
- 交互功能测试
- 诊断和故障排查

## 环境准备

### 前置条件
- Node.js 16+
- npm 或 yarn
- 现代浏览器（Chrome/Firefox/Edge）
- 服务器正确启动

### 检查服务器状态
```bash
# 检查服务器是否运行
curl http://localhost:8787/api/settings

# 预期响应：
# {"ok":true,"settings":{...}}
```

## 第一部分：基本功能测试

### 1.1 访问拓扑还原页面

1. 打开浏览器，导航到拓扑还原页面
2. 页面应显示：
   - 网络类型选择（IB/RoCE）
   - 拓扑检测配置面板
   - 文件上传区域
   - 将来的：拓扑图显示区域

### 1.2 配置面板测试

**测试自动识别模式：**
- 默认选择"自动识别（推荐）"
- 确保选项可切换
- 验证说明文字正确显示

**测试手动配置模式：**
1. 切换到"手动配置"
2. 点击"显示高级选项"
3. 验证正则表达式输入框出现
4. 输入测试正则表达式：
   - Core: `IBCR|CORE`
   - Spine: `IBSP|SPINE`
   - Leaf: `IBLF|LEAF`
5. 验证 POD 分组选项可用

### 1.3 文件上传测试

**Test IB Network (UFM CSV):**
1. 选择"IB 网络 (InfiniBand)"
2. 使用 `test-data/ib-topology-sample.csv` 文件
3. 点击"还原拓扑"按钮
4. 验证加载状态显示"解析中..."

**预期结果：**
- ✅ 显示 "成功解析 X 个节点, Y 条连接"
- ✅ 拓扑图正确显示
- ✅ 节点按层级分组（core/spine/leaf）
- ✅ 边正确连接节点
- ✅ POD 选择器显示可用的 POD

## 第二部分：IB 拓扑深入测试

### 2.1 拓扑结构验证

对于 `ib-topology-sample.csv`：

**预期数据统计：**
```
Core (IBCR):    5 个设备
Spine (IBSP):   10 个设备（POD1: 5个, POD2: 5个）
Leaf (IBLF):    30 个设备（POD1: 15个, POD2: 15个）
总计:            45 个节点
连接:            约 80 条
```

**验证步骤：**
1. 打开浏览器开发者工具（F12）
2. 转到 Console 标签页
3. 查找日志信息：
   ```
   [TopologyRestore] Result: {...}
   [TopologyRestore] nodesByLayer 内容: {...}
   [TopologyRestore] 拓扑渲染完成: {
     nodeCount: 45,
     edgeCount: 80,
     sampleNodes: [...],
     nodesByLayerKeys: ['core', 'spine', 'leaf']
   }
   ```

### 2.2 POD 选择功能

1. 验证 POD 选择器显示：`ALL`, `POD1`, `POD2`
2. 选择 `POD1`：
   - 应只显示 POD1 的节点
   - Core 层（5个）应完全显示
   - Spine 层（5个）应显示 POD1 相关
   - Leaf 层（15个）应显示 POD1 相关
3. 选择 `POD2`：
   - 应只显示 POD2 的节点
4. 选择 `ALL`：
   - 应显示所有节点

### 2.3 层级可见性控制

1. 验证层级复选框显示：CORE, SPINE, LEAF
2. 取消选中 CORE：
   - 应只显示 Spine 和 Leaf
   - 连接应自动过滤
3. 取消选中 SPINE：
   - 仅显示 Leaf 层
   - Core 和 Leaf 之间无连接
4. 恢复所有层级可见

### 2.4 拓扑结构验证

**布局验证：**
- ✅ Core 层在顶部（Y=100）
- ✅ Spine 层在中间（Y=300）
- ✅ Leaf 层在底部（Y=500）
- ✅ 同层节点水平分布均匀
- ✅ 节点有适当的间距（无重叠）

**连接线验证：**
- ✅ 所有连接线正确连接源和目标节点
- ✅ 连接线显示端口信息（如："1-1"）
- ✅ 连接方向正确（箭头指向目标）

## 第三部分：交互功能测试

### 3.1 节点点击

1. 在拓扑图中点击一个 Leaf 节点
2. 验证右侧信息面板显示：
   - 设备 ID（如 `IBLF-POD1-001`）
   - 层级标签（LEAF）
   - 位置信息（X, Y 坐标）
   - 连接关系（显示连接到此节点的所有边）

3. 验证连接关系格式：
   ```
   → IBSP-POD1-001    (如果是源设备)
   ← IBSP-POD1-001    (如果是目标设备)
   端口: 1-1
   ```

### 3.2 边点击

1. 点击一条连接线
2. 验证右侧信息面板显示：
   - 源设备 ID
   - 目标设备 ID
   - 端口信息（格式：`1-2` 表示源端口 1 到目标端口 2）
   - 链路概览（源→目标的连接图）

### 3.3 搜索功能

1. 在搜索框输入 `IBLF-POD1-001`
2. 点击搜索按钮或按 Enter
3. 验证：
   - ✅ 节点被高亮显示（红色边框和发光效果）
   - ✅ 显示消息 "找到设备: IBLF-POD1-001"
   - ✅ 视图自动调整到节点位置（fitView）

4. 测试搜索不存在的设备：
   - 输入 `NONEXISTENT-001`
   - 应显示 "未找到匹配的设备"

### 3.4 刷新按钮

1. 点击右侧的刷新按钮（↻ 图标）
2. 验证：
   - ✅ 拓扑重新渲染
   - ✅ 所有节点和边正确显示
   - ✅ 当前的 POD 和层级选择保持不变

### 3.5 画布交互

1. **缩放**：使用鼠标滚轮或触板
   - 应可放大/缩小拓扑
   - 防止缩放超出合理范围

2. **平移**：按住中键或Ctrl+鼠标左键拖动
   - 应能在画布上自由移动

3. **点击空白处**：
   - 应清除节点/边的选择
   - 右侧信息面板应显示默认提示

## 第四部分：性能测试

### 4.1 加载时间

对于 45 个节点、80 条边的拓扑：
- 前端消息到结果显示：< 2 秒
- 开发者工具 Network 标签显示的响应时间：< 1 秒

### 4.2 交互响应

- 选择节点：< 100ms
- 切换 POD：< 500ms
- 切换层级可见性：< 500ms
- 搜索：< 100ms

### 4.3 内存使用

- 初始加载：< 50MB
- 与不同 POD 交互：无显著增长
- 长时间使用：无内存泄漏

## 第五部分：故障排查

### 5.1 拓扑不显示

**诊断步骤：**
1. F12 → Console，查找错误信息
2. 验证 Network 请求：
   - 请求状态码是否为 200
   - 响应中是否包含 `nodesByLayer`
3. 运行诊断脚本（见 TOPOLOGY_NO_DISPLAY_DEBUG.md）

**常见原因和解决方案：**
- 📍 **nodesByLayer 为空**
  - 检查 CSV/Excel 文件格式
  - 验证表头行正确
  - 确保设备名称正确识别

- 📍 **某些层没有节点**
  - 检查自动检测算法
  - 尝试手动配置层级识别规则
  - 验证数据中确实包含该层设备

- 📍 **节点位置不正确（重叠）**
  - 已通过坐标计算修复
  - 若仍有问题，检查 buildTopology 日志

### 5.2 连接线显示不完整

**检查点：**
1. 验证源和目标节点都已创建
2. 查看 Console 中的 edgeCount
3. 检查节点 ID 大小写是否匹配

### 5.3 UI 控件无响应

**故障排查：**
1. 检查浏览器控制台是否有 JavaScript 错误
2. 验证 restoreResult 是否正确设置
3. 检查 layerVisibility 是否初始化

## 第六部分：手动配置层级测试

### 6.1 自定义正则表达式

对于非标准命名的网络：

**示例 1：使用数字前缀**
- Core: `^CORE\d+`
- Spine: `^SP\d+`
- Leaf: `^LF\d+`

**示例 2：使用后缀标识**
- Core: `CORE$`
- Spine: `SPINE$`
- Leaf: `LEAF$`

### 6.2 验证手动配置有效性

1. 更改正则表达式
2. 点击"还原拓扑"
3. 验证节点正确分类到不同层级

## 第七部分：回归测试清单

每次修改后运行此清单：

- [ ] ✅ IB 拓扑加载和显示正确
- [ ] ✅ 所有 45 个节点可见
- [ ] ✅ 所有 80 条连接正确
- [ ] ✅ POD 选择功能正常
- [ ] ✅ 层级可见性控制正常
- [ ] ✅ 节点点击显示信息
- [ ] ✅ 边点击显示信息
- [ ] ✅ 搜索功能正常
- [ ] ✅ 刷新按钮有效
- [ ] ✅ 画布缩放和平移正常
- [ ] ✅ 无控制台错误
- [ ] ✅ 无内存泄漏
- [ ] ✅ 响应时间在可接受范围

## 第八部分：网络请求验证

### 8.1 观察 API 请求

1. F12 → Network 标签
2. 点击"还原拓扑"
3. 找到 POST `/api/topology-restore` 请求
4. 查看 Response 内容，验证包含：
   ```json
   {
     "ok": true,
     "nodesByLayer": {
       "core": [...],
       "spine": [...],
       "leaf": [...]
     },
     "connections": [...],
     "metadata": {
       "pods": ["POD1", "POD2"],
       "layers": {
         "core": [...],
         "spine": [...],
         "leaf": [...]
       },
       "stats": {
         "totalDevices": 45,
         "coreCount": 5,
         "spineCount": 10,
         "leafCount": 30,
         "avgDegree": "8.89"
       }
     },
     "nodeCount": 45,
     "edgeCount": 80
   }
   ```

### 8.2 验证配置传递

1. 在 Network 请求的 Request 中查看 FormData
2. 验证字段：
   - `file`: 上传的文件
   - `networkType`: `ib` 或 `roce`
   - `config`: JSON 配置字符串

## 第九部分：批量测试场景

### 场景 1：多 POD 网络
测试具有 3-4 个 POD 的更大网络
- 验证 POD 选择器显示所有 POD
- 验证选择每个 POD 时正确过滤

### 场景 2：非对称拓扑
测试具有不同设备数量的层级
- 核心：2 个设备
- Spine：4 个设备
- Leaf：20 个设备

### 场景 3：高密度拓扑
测试具有大量连接的网络
- 验证绘制性能
- 验证交互响应

## 第十部分：文档和日志

### 10.1 开发者工具输出

保存完整的控制台输出用于调试：
```bash
# 在控制台中执行
copy(document.documentElement.innerHTML)
# 粘贴到文件中保存
```

### 10.2 服务器日志

服务器应输出：
```
[TopologyRestore] 开始解析 ib 网络拓扑，文件: ib-topology-sample.csv
[TopologyRestore] CSV解析完成: 80 条端口映射
[TopologyRestore] 拓扑解析完成: 45 设备, 80 链路
```

## 总结

完成此测试指南验证：
1. ✅ 拓扑还原核心功能正常工作
2. ✅ UI 控件和交互功能完整
3. ✅ 自动和手动配置都可用
4. ✅ 性能在可接受范围
5. ✅ 故障排查工具和文档完备

---

**最后更新**: 2025-12-28
**版本**: 1.0
